info:
  name: service health
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8081/actuator/health/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
