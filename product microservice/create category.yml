info:
  name: create category
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8081/categories
  body:
    type: json
    data: |2-
        {
          "name": "Fruits",
          "description": "Apples, bananas, pears"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
