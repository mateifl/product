info:
  name: get categories
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
